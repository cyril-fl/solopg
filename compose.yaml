services:
  solopg-mongo:
    image: docker.io/library/mongo:latest
    
    container_name: solopg-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
      - mongo-config:/data/configdb

volumes:
  mongo-data:
  mongo-config:
